1. After clone "npm install"
2. Run project "mvn clean install" build and test
3. After Run it "mvn spring-boot:run" for run the project

Noted: If your computer can run without build again is great, but is not like macbook just after edit code let use step 2 & 3 after you edit code 
